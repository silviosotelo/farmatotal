# API (Fastify + Drizzle). Deriva de ft-base (deps ya instaladas). Corre con tsx.
FROM ft-base
WORKDIR /app/apps/api
EXPOSE 4000
# NODE_ENV se setea en runtime (compose).
CMD ["pnpm", "exec", "tsx", "src/index.ts"]
