# Referencia nativa (congelada)

Snapshot de las páginas/componentes NATIVOS del storefront tomado antes de pasar
todo al builder (commit con fallback aún activo). Sirve como referencia pixel-perfect
para reconstruir los bloques del builder. NO se compila ni se incluye en el build
(fuera de src/, en .dockerignore, excluido en tsconfig).
